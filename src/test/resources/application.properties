spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# >=256-bit secret for HS256 (32+ bytes recommended)
jwt.secret=3z6n9q2s5v8y1a4d7k0f3l9p6r2t5w8u
jwt.expiration=3600000

# Keep tests focused on JPA/H2. Flyway is exercised in dev/prod.
spring.flyway.enabled=false

# Refresh token lifetime (keep consistent with app default).
app.refresh-token.expiration-ms=2592000000

# Avoid flaky tests due to per-IP rate limits.
app.rate-limit.enabled=false
