spring.application.name=bakery-api

jwt.secret=${JWT_SECRET:ClaveDeDesarrolloMuySegura123}
jwt.expiration=86400000

# Datos de la base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/bakery_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=admin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
